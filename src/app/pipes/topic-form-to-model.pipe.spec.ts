import { TopicFormToModelPipe } from './topic-form-to-model.pipe'

describe('TopicFormToModelPipe', () => {
	it('create an instance', () => {
		const pipe = new TopicFormToModelPipe()
		expect(pipe).toBeTruthy()
	})
})
