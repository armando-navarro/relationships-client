import { NewlinesToBrPipe } from './newlines-to-br.pipe'

describe('NewlinesToBrPipe', () => {
	it('create an instance', () => {
		const pipe = new NewlinesToBrPipe()
		expect(pipe).toBeTruthy()
	})
})
